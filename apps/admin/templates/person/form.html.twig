{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Person' %}
{% set formCancelLink = path('admin_person_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Person</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
