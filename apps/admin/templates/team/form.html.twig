{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Team' %}
{% set formCancelLink = path('admin_team_index') %}

{% block pageBody %}
    {{ component('admin_team_form', {
        team: object|default(null),
        form: form
    }) }}
{% endblock %}
