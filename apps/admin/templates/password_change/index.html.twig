{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Passwort ändern' %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Passwort ändern</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
