{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'User' %}
{% set formCancelLink = path('app_admin_user_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">User</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
