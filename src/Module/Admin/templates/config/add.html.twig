{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pagePreTitle = 'Add' %}
{% set pageTitle = 'Configs' %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Settings</h3>
        </div>
        <div class="card-body">
            {{ form_row(form) }}
        </div>
    </div>
{% endblock %}
