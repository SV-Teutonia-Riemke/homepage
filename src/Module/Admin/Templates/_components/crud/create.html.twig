{% if crud is crud_has_create %}
    {% component 'bs:button' with {
        type: 'primary',
        icon: 'fa6-solid:plus',
        href: crud_url_create(crud),
        title: title,
    } %}
    {% endcomponent %}
{% endif %}
