{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Menu' %}
{% set formCancelLink = path('app_admin_menu_item_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Menu</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
