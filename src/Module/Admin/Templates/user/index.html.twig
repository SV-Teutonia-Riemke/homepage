{% extends '@admin/_layout/layout.html.twig' %}

{% set pagePreTitle = 'Liste' %}
{% set pageTitle = 'Benutzer' %}

{% block pageTitleActionsButtons %}
    <twig:crud:create crud="{{ crud }}" />
{% endblock %}

{% block pageBody %}
    <twig:pagination:table :title="pageTitle">
        <twig:block name="tableHeader">
            <th class="w-1">Id</th>
            <th>Email</th>
            <th></th>
        </twig:block>
        <twig:block name="tableRow">
            <twig:crud:row>
                <td>{{ object.id }}</td>
                <td>{{ object.email }}</td>
            </twig:crud:row>
        </twig:block>
    </twig:pagination:table>
{% endblock %}
