{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Article' %}
{% set formCancelLink = path('app_admin_article_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Article</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
