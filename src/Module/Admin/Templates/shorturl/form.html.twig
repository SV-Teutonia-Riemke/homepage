{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Short URL' %}
{% set formCancelDownload = path('app_admin_shorturl_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Short URL</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
