{% extends '@admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Benachrichtigung' %}
{% set formCancelLink = path('app_admin_notification_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Notification</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
