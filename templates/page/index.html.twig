{% extends '_layout/layout.html.twig' %}

{% block pageBody %}
    <div class="row row-cards">
        <div class="col-lg-9">
            <div class="col-12">
                {{ component('alert', {
                    message: 'I am a success alert!',
                }) }}

                {% for article in articles %}
                    <div class="card mb-3">
                        <div class="card-header">
                            <div class="card-title">{{ article.title }}</div>
                        </div>
                        <div class="card-body">{{ article.content|raw }}</div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="col-md-3">
            <div class="row row-cards">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header border-0">
                            <div class="card-title">Sponsoren</div>
                            <div class="card-body">
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-12">
                    <div class="card">
                        <div class="card-header border-0">
                            <div class="card-title">Spenden</div>
                            <div class="card-body"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
