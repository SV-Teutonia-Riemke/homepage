<{{ this.rootTag }}{{ attributes.defaults(this.defaults) }}>
    {% block content %}
        {% if icon is not null %}
            <twig:ux:icon :name="icon" />
        {% endif %}
        {% if title is not null %}
            {{ title }}
        {% endif %}
    {% endblock %}
</{{ this.rootTag }}>
