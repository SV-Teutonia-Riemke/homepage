<div class="alert alert-{{ type }} alert-dismissible" role="alert">
    {% if icon is not null %}
        <span class="iconify" data-icon="{{ icon }}"></span>
    {% endif %}
    {{ message }}
</div>
