{% extends 'admin/_layout/layout.html.twig' %}

{% set pagePreTitle = 'Create' %}
{% set pageTitle = 'Users' %}

{% block pageBody %}
    {{ form_start(form) }}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Users</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
        <div class="card-footer text-end">
            <div class="d-flex">
                <a href="{{ path('app_admin_user_index') }}" class="btn btn-link">Cancel</a>
                <button type="submit" class="btn btn-primary ms-auto">Submit</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
