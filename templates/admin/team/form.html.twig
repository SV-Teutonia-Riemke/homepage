{% extends 'admin/_layout/simple_form.html.twig' %}

{% set pageTitle = 'Team' %}
{% set formCancelLink = path('app_admin_team_index') %}

{% block pageBody %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Team</h3>
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
